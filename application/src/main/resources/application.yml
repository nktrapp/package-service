spring:
  application:
    name: package-service
  jackson:
    default-property-inclusion: non_null
    datatype:
      datetime:
        write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,loggers
      base-path: /management
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
  health:
    mongodb:
      enabled: true
  info:
    env:
      enabled: true
    git:
      enabled: true

server:
  port: 8081
  shutdown: graceful
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    root: INFO
    br.furb: INFO
    io.awspring.cloud: INFO
    org.springframework.data.mongodb: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

app:
  messaging:
    inbound-queue: ${APP_MESSAGING_INBOUND_QUEUE:logistics-events-queue.fifo}
    outbound-queue: ${APP_MESSAGING_OUTBOUND_QUEUE:package-events-queue.fifo}
  outbox:
    relay:
      fixed-delay-ms: 2000
      batch-size: 50
      max-attempts: 5
      retry-delay-ms: 5000
      processing-timeout-ms: 60000
    retention:
      fixed-delay-ms: 3600000        # purge sweep interval (1h)
      published-retention-ms: 604800000  # keep published events 7 days
